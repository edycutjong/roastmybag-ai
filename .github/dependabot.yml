version: 2

# This project is a completed, judged hackathon submission with a live
# production deploy. The repo is a frozen artifact, so dependency VERSION
# updates are intentionally disabled — churn here can only break the demo
# that judges click, and buys nothing in return.
#
# Dependabot SECURITY alerts and automated security fixes are a separate
# repo setting and remain ENABLED. Vulnerabilities still get reported and
# patched; routine version bumps do not.
updates: []
